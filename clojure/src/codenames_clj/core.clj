(ns codenames-clj.core
  (:gen-class))

(defn -main
  "I don't do a whole lot ... yet."
  [& args]
  (println "Hello, World!"))

(defn get-wordlist []


  )

(defn generate-gameboard
  (->> get-wordlist
      ))
